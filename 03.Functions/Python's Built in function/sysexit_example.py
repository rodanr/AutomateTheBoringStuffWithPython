import sys # Importing the sys module
"""
sys.exit() is the function that terminates the program running whenever the interpreter finds this function 
is called.
To use sys.exit() we need to import the python standard library i.e "import sys"
"""
print("Hello,World !")
sys.exit() # Program running gets terminated here and line 9 doesn't run now
print("Bye,World !")